streamlit>=1.32
requests>=2.31
jinja2>=3.1

# Codebook generator core
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5
python-multipart>=0.0.9

# Document fallback (optional; only imported when --pdf / --docx used)
pdfplumber>=0.10
python-docx>=1.1

# Output formats
openpyxl>=3.1

# Optional LLM + Notion sync (only required when ANTHROPIC_API_KEY / NOTION_API_KEY are set)
anthropic>=0.40
notion-client>=2.2
