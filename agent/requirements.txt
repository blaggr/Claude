# The agent runs fully offline with zero dependencies (stdlib only).
# These are optional and only activate the corresponding upgraded paths:
anthropic>=0.40    # reasoning with Claude (else the offline heuristic policy drives the loop)
yfinance>=0.2      # delayed quotes (else live Alpaca IEX, else the offline price stub)
