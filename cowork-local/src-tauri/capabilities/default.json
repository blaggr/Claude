{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set: core defaults plus the folder-picker dialog. No filesystem or shell plugins are granted to the webview; all privileged work goes through vetted Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
