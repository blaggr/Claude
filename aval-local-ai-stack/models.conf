# AVAL Local AI Stack: model manifest.
# One model family across tiers, so behavior stays as close as hardware allows.
# The installer picks the row with the largest MIN_RAM_GB <= this machine's RAM.
#
# Columns: TIER  MIN_RAM_GB  MODEL_TAG  EXPECTED_ID
# EXPECTED_ID = first 12 hex chars from `ollama list` on the maintainer's
# tested machine. "-" = not yet pinned (verify warns until it is filled in).
#
# Memory fit is an estimate. Confirm in the pilot.
1   8    qwen3.5:4b        -
2   16   qwen3.5:9b        -
3   32   qwen3.6:27b       -
4   64   qwen3.6:35b-a3b   -
